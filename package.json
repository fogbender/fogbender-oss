{
  "private": true,
  "scripts": {
    "release": "changeset version && yarn install && yarn fmt",
    "build": "yarn workspace fogbender build && yarn workspace fogbender-react build && yarn workspace fogbender-vue build && yarn workspace fogbender-proto build",
    "fmt": "prettier --write '**/*.{ts,js,tsx,jsx,css,json,md,prettierrc,html,yml}'"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.24.4"
  }
}
