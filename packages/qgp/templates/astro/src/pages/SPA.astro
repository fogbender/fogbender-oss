---
import Layout from "../layouts/SPA-layout.astro";
import { App } from "../components/ReactSPA";
---

<Layout title="">
  <script>
    window.global = window;
    window.process = window.process || { env: import.meta.env || { NODE_ENV: "production" } };
  </script>
  <App client:only="react" />
</Layout>
